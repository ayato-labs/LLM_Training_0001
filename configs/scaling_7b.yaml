# 7B - RTX 5090 x2 (48GB)
defaults:
  - config

model:
  target_params: 7_000_000_000
  llama:
    hidden_size: 4096
    num_hidden_layers: 32
    num_attention_heads: 32
    intermediate_size: 11008

training:
  seq_len: 4096
  num_epochs: 3
