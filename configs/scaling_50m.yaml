# 50M - RTX 3050 (4GB)
defaults:
  - config

model:
  target_params: 50_000_000
  llama:
    hidden_size: 640
    num_hidden_layers: 10
    num_attention_heads: 10
    intermediate_size: 2560
    rope_theta: 50000.0

training:
  seq_len: 2048
  max_steps: 1500
  num_epochs: 1
