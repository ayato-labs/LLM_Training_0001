# 学習シーケンス図

```mermaid
sequenceDiagram
    participant B as run_hpo.bat
    participant H as scripts/find_hparams.py
    participant SL as src/hpo/step_law.py
    participant O as Optuna (Proxy HPO)
    participant PT as src/training/train_model.py (Proxy)
    participant T as run_train.bat
    participant M as src/training/main.py
    participant DS as GPU/CPU
    participant TB as TensorBoard
    participant GD as Google Drive

    %% Phase 1: Offline HPO
    Note over B,PT: Phase 1: Offline HPO (探索フェーズ)
    B->>H: --model-size 150M --data-path data/dataset.jsonl --n-trials 100
    H->>SL: compute_hpo_for_target(n_params, n_tokens, seq_len)
    SL-->>H: Step Law Priors (max_lr_2d, max_lr_1d, batch_seqs, warmup)
    H->>H: データセットをメモリ上で一度だけトークナイズ (data_fraction=0.001)
    H->>O: Optuna study作成 (MedianPruner)
    
    loop n_trials 回
        O->>O: suggest ハイパラサンプリング (max_lr_2d, max_lr_1d, batch, weight_decay)
        O->>PT: proxy_train(config, tokenized_dataset, pruning_callback)
        PT->>PT: CustomTrainer構築 (Muon for 2D + AdamW for 1D)
        loop 50ステップ
            PT->>DS: 1ステップ学習
            DS-->>PT: Loss返却
            PT->>O: OptunaPruningCallbackでLoss報告
            alt Pruning判定 (should_prune)
                O-->>PT: TrialPruned例外
                PT->>PT: 中断・クリーンアップ
            end
        end
        PT->>PT: 保存先モデル・中間チェックポイントを完全削除 (ディスク節約)
        PT-->>O: 最終Loss報告
    end
    
    O-->>H: best_params
    H->>H: 派生値計算 (per_device_batch_size, grad_accum_steps)
    H->>H: YAML出力 (configs/hparams_150M.yaml)
    H-->>B: 完了

    %% Phase 2: Online Training
    Note over T,GD: Phase 2: Online Training (学習フェーズ)
    T->>M: python -m src.training.main (Hydra合成: config.yaml + hparams_150M.yaml)
    M->>M: load_config()
    
    alt resume_from_checkpoint 指定あり
        M->>M: hashes.jsonの検証 (config/dataのハッシュ一致を確認、不一致ならエラー)
    end
    
    M->>M: Tokenizer読込 (local tokenizer.json) & ADR-021特殊トークン設定
    M->>M: Dataset読込・Tokenize (リソースに応じてnum_proc自動調整)
    M->>M: LlamaForCausalLM初期化 (Dynamic Config)
    M->>M: HF Trainer構築 (Cosine Scheduler & callbacks)
    
    loop 各ステップ
        M->>DS: 1ステップ学習 (Gradient Accumulation)
        DS-->>M: Loss, LR, Grad Norm
        M->>TB: ログ記録 (10ステップ毎)
        M->>M: Checkpoint保存 (1000ステップ毎) & hashes.json出力
        opt DriveUploadCallback
            M->>GD: チェックポイントをZip圧縮してGoogle Driveへ非同期アップロード (1000ステップ毎)
        end
    end
    
    M->>M: Final Model & Tokenizer保存 (models/output/)
    opt DriveUploadCallback
        M->>GD: 最終チェックポイントを強制アップロード
    end
    M-->>T: 完了
```

## 説明

### Phase 1: Offline HPO (探索フェーズ)
- **完全分離**: 本番学習コード(`src/training/main.py`)は `optuna` を依存・import しない
- **Step Law Prior**: Chinchilla則(LR ∝ N^-0.713) + μP則(2D/1D LR分離)から探索中心を決定
- **効率的トークナイズ**: 各試行ごとではなく、最適化開始前にメモリ上でデータセットのトークナイズを一括で実行
- **プロキシ学習**: `src/training/train_model.py` 内の `CustomTrainer` を用いて、50ステップ、データ0.1%で高速評価
- **オプティマイザ分割**: 2Dパラメータには `Muon` (ない場合は `AdamW`)、1Dパラメータには `AdamW` を適用
- **自動クリーンアップ**: 試行ごとにディスクを圧迫するチェックポイントや出力を完全削除
- **成果物**: `configs/hparams_<SIZE>.yaml` を出力しGit管理

### Phase 2: Online Training (学習フェーズ)
- **即時開始**: 起動から学習開始まで高速
- **整合性検証 (ADR)**: チェックポイントから再開する際、`hashes.json` に保存された config とデータセットの SHA256 ハッシュを検証し、変更があればエラーで処理を中断
- **Hydra合成**: `config.yaml` (人間編集) + `hparams_150M.yaml` (最適ハイパラ) = 実行時設定
- **Tokenizer**: ローカルの `tokenizer.json` から直接読み込み、ADR-021に基づき SP-native トークン名 (IDs 0-3) を手動設定
- **リソース自動調整**: CPUコア数および空きRAM容量から最適なデータ処理プロセス数 `num_proc` を動的算出
- **Trainer**: 標準の Hugging Face `Trainer` と `Cosine` スケジューラを使用して本番学習を実行
- **定期バックアップ**: `DriveUploadCallback` により、1000ステップごとにチェックポイントをzip圧縮して Google Drive に非同期アップロード
- **依存最小**: `torch`, `transformers`, `datasets`, `tensorboard` のみ (DeepSpeed/Muonは環境に応じて利用)