# 実装計画書: LLM_Training リファクタリング（引き算のエンジニアリング）詳細版

## 1. 目的・背景・制約条件

### 1.1 制約条件（ハード制約）
- **ハードウェア**: 個人GPU（VRAM 4GB〜24GB、単一GPU前提）
- **ストレージ**: ローカルSSD容量逼迫 → チェックポイントはGoogle Driveへ自動退避必須
- **人員**: 単独開発・保守
- **時間**: 学習実行までのリードタイム最小化

### 1.2 現状の課題
| 課題 | 影響 | 根本原因 |
|---|---|---|
| 学習開始まで3〜5分かかる | 実験回転率低下 | 起動時Optuna HPO（proxy学習含む）実行 |
| 依存関係が重い | 環境構築・CI困難 | `optuna`, `mlflow`, `deepspeed`, `muon` 等が必須依存 |
| 独自実装の保守コスト | バグ修正に時間 | `modern_gpt.py`, `CustomTrainer`, `Muon` 等 |
| 設定ファイル乱立 | どれ使うか�う | `scaling_*.yaml` 4種類 + `config.yaml` |
| ログ二重管理 | 確認手間・容量 | MLflow + TensorBoard 同時書き込み |

### 1.3 リファクタリング原則（引き算）
1. **標準ライブラリへの委譲**: HF `Trainer` + `LlamaForCausalLM` + `AdamW` で代替可能な独自コードは削除
2. **実行時計算の事前計算化**: HPO・Step Law は「学習前のオフライン処理」として分離し、結果（YAML）のみを学習時に読み込み
3. **単一責任・単一エントリーポイント**: `main.py` 1ファイルで「設定→データ→モデル→学習→保存」完結
4. **ログ基盤の統一**: TensorBoardのみ（ローカル完結・軽量・VSCode統合済み）
5. **例外**: Google Driveアップロード（SSD容量確保）は維持・強化

---

## 2. ファイルレベル詳細アクションプラン

### 2.1 物理削除（`git rm` 対象）

```bash
# 独自モデル実装（HF標準で置換）
git rm src/modern_gpt.py src/registry.py

# 独自Trainer・Optimizer（標準Trainer+AdamWで置換）
git rm src/trainer.py

# 未使用・重複解析スクリプト
git rm src/scaling_analysis.py

# 設定ファイル統合（hparams分離方式へ移行）
git rm configs/scaling_50m.yaml configs/scaling_150m.yaml configs/scaling_3b.yaml configs/scaling_7b.yaml
```

**削除後の `src/` 目標構成（10ファイル以内）:**
```
src/
├── __init__.py
├── main.py              # エントリーポイント（統合後 ~200行）
├── config.py            # Hydra設定ローダー
├── drive_uploader.py    # 維持・強化
├── logger.py            # 維持
├── set_seed.py          # 維持
├── env_snapshot.py      # 簡素化
├── model_utils.py       # 維持・整理
├── hpo_manager.py       # ライブラリ化（mainからimportされない）
├── step_law.py          # ライブラリ化（mainからimportされない）
├── prepare_dataset.py   # 維持
├── extract_data.py      # 維持
├── split_dataset.py     # 維持
├── train_tokenizer.py   # 維持
└── check_gpu.py         # 維持
```

### 2.2 `src/main.py` 完全書き換え仕様

#### 目標コード構造（疑似コード）
```python
#!/usr/bin/env python3
"""
LLM Training Entry Point
Usage: python -m src.main [--config-name CONFIG] [OVERRIDES...]
Examples:
  python -m src.main                    # config.yaml + hparams_150M.yaml
  python -m src.main max_steps=100      # デバッグ用短縮実行
  python -m src.main +experiment=debug  # 実験用オーバーライド
"""

import sys
import hydra
from omegaconf import DictConfig, OmegaConf
from pathlib import Path

import torch
from transformers import (
    AutoTokenizer,
    LlamaForCausalLM,
    Trainer,
    TrainingArguments,
    DataCollatorForLanguageModeling,
)
from datasets import load_dataset

from src.config import load_config
from src.logger import logger, log_exceptions
from src.set_seed import set_seed
from src.env_snapshot import capture_env_snapshot
from src.drive_uploader import DriveUploadCallback
from src.model_utils import estimate_model_size, create_model_config


@hydra.main(version_base=None, config_path="../configs", config_name="config")
@log_exceptions
def main(cfg: DictConfig) -> None:
    # 1. 設定正規化（DictConfig → 通常dict + 検証）
    config = load_config(cfg)
    logger.info(f"Config resolved: {OmegaConf.to_yaml(config)}")

    # 2. 環境スナップショット取得（再現性）
    env_snap = capture_env_snapshot()
    logger.debug(f"Env: {env_snap}")

    # 3. Seed固定
    set_seed(config.seed, deterministic=True)

    # 4. Tokenizer
    tokenizer = AutoTokenizer.from_pretrained(config.tokenizer_path)
    # SP互換特殊トークン設定
    tokenizer.unk_token = "ᠨ"
    tokenizer.bos_token = "ᠠ"
    tokenizer.eos_token = "ᠡ"
    tokenizer.pad_token = "<pad>"

    # 5. Dataset
    train_ds, eval_ds = load_and_tokenize_datasets(config, tokenizer)

    # 6. Model
    model = create_model(config, tokenizer)

    # 7. DeepSpeed Config（VRAM自動検出ベース）
    ds_config = resolve_deepspeed_config(config, model)
    
    # 8. TrainingArguments
    args = build_training_args(config, ds_config)

    # 9. Trainer + Callbacks
    callbacks = [DriveUploadCallback(upload_interval_steps=config.drive_upload_interval)]
    
    trainer = Trainer(
        model=model,
        args=args,
        train_dataset=train_ds,
        eval_dataset=eval_ds,
        data_collator=DataCollatorForLanguageModeling(tokenizer, mlm=False),
        callbacks=callbacks,
    )

    # 10. Train
    logger.info("*** Starting Training ***")
    trainer.train(resume_from_checkpoint=config.resume_from_checkpoint)

    # 11. Save Final
    trainer.save_model(config.output_dir)
    tokenizer.save_pretrained(config.output_dir)
    logger.info(f"Model saved to {config.output_dir}")

    # 12. Final Drive Upload
    callbacks[0].force_final_upload(trainer.state.global_step)


def load_and_tokenize_datasets(config, tokenizer):
    """データ読み込み・トークナイズ・分割"""
    data_files = {"train": config.data_path}
    if config.get("val_data_path"):
        data_files["validation"] = config.val_data_path
    
    ds = load_dataset("json", data_files=data_files)
    
    def tokenize_fn(examples):
        return tokenizer(
            examples["text"],
            padding="max_length",
            truncation=True,
            max_length=config.seq_len,
        )
    
    ds = ds.map(tokenize_fn, batched=True, remove_columns=["text", "metadata"])
    
    # data_fraction for pilot (if specified)
    if config.get("data_fraction", 1.0) < 1.0:
        for split in ds:
            n = int(len(ds[split]) * config.data_fraction)
            ds[split] = ds[split].select(range(n))
    
    ds.set_format("torch")
    return ds["train"], ds.get("validation")


def create_model(config, tokenizer):
    """LlamaForCausalLM 初期化"""
    model_config = create_model_config(config, tokenizer)
    model = LlamaForCausalLM(model_config)
    model.resize_token_embeddings(len(tokenizer))
    return model


def build_training_args(config, ds_config_path):
    """TrainingArguments 構築"""
    return TrainingArguments(
        output_dir=config.output_dir,
        learning_rate=config.max_lr_2d,
        per_device_train_batch_size=config.per_device_batch_size,
        gradient_accumulation_steps=config.grad_accum_steps,
        gradient_checkpointing=True,
        max_steps=config.max_steps,
        num_train_epochs=config.num_epochs if config.max_steps == -1 else 0,
        lr_scheduler_type="cosine",
        warmup_ratio=config.warmup_ratio,
        weight_decay=config.weight_decay,
        adam_beta2=config.beta2,
        max_grad_norm=config.grad_clip,
        bf16=config.precision == "bf16",
        fp16=config.precision == "fp16",
        deepspeed=ds_config_path if ds_config_path else None,
        save_strategy="steps",
        save_steps=config.save_steps,
        eval_strategy="steps" if config.get("val_data_path") else "no",
        eval_steps=config.eval_steps if config.get("val_data_path") else None,
        logging_steps=config.logging_steps,
        report_to=["tensorboard"],
        load_best_model_at_end=bool(config.get("val_data_path")),
        metric_for_best_model="eval_loss",
        greater_is_better=False,
        seed=config.seed,
        remove_unused_columns=False,
    )


def resolve_deepspeed_config(config, model):
    """VRAM検出→ZeRO Stage決定→JSON生成→パス返却"""
    from src.model_utils import generate_deepspeed_config
    vram_gb = config.get("vram_limit_gb") or detect_vram()
    n_params = estimate_model_size(model)
    return generate_deepspeed_config(n_params, vram_gb, config.precision)


def detect_vram() -> float:
    try:
        if torch.cuda.is_available():
            return round(torch.cuda.get_device_properties(0).total_memory / 1024**3, 2)
    except Exception:
        pass
    return 4.0  # Safe fallback


if __name__ == "__main__":
    main()
```

#### 削除対象関数（現行 `main.py` から完全除去）
- `run_pilot_check()` 
- `apply_step_law()`
- `optuna.study.optimize()` ブロック全体
- MLflow関連の全ロジック（`mlflow.start_run`, `log_params`, `log_dict`, `log_artifact` 等）

### 2.3 `src/config.py` 書き換え仕様

#### 新インターフェース
```python
"""Config Loader: Hydra DictConfig → 内部dict 正規化"""

from pathlib import Path
from omegaconf import DictConfig, OmegaConf
import torch


def load_config(cfg: DictConfig) -> dict:
    """
    Hydra合成済みDictConfigをフラットなdictに変換・検証。
    必須キー: model_params, training, data_path, tokenizer_path, output_dir, seed
    """
    # 構造化設定への変換
    container = OmegaConf.to_container(cfg, resolve=True, throw_on_missing=True)
    
    # 正規化・デフォルト値補完
    normalized = _normalize_config(container)
    
    # VRAM自動検出（上書き可能）
    if "vram_limit_gb" not in normalized or normalized["vram_limit_gb"] is None:
        normalized["vram_limit_gb"] = _detect_vram()
    
    # Precision既定値
    normalized.setdefault("precision", "bf16")
    
    # 出力ディレクトリ解決
    normalized["output_dir"] = str(Path(normalized.get("output_dir", "models/output")).resolve())
    
    return normalized


def _normalize_config(raw: dict) -> dict:
    """ネストしたYAML構造を学習パイプライン用フラットdictに変換"""
    
    # model_params 抽出
    model = raw.get("model", {})
    llama = model.get("llama", {})
    model_params = {
        "n_params": model.get("target_params", 150_000_000),
        "hidden_size": llama.get("hidden_size", 768),
        "num_hidden_layers": llama.get("num_hidden_layers", 12),
        "num_attention_heads": llama.get("num_attention_heads", 12),
        "num_key_value_heads": llama.get("num_key_value_heads", 3),
        "intermediate_size": llama.get("intermediate_size", 3072),
        "rope_theta": llama.get("rope_theta", 10000.0),
        "vocab_size": llama.get("vocab_size", 64000),
    }
    
    # training 抽出（hparams_*.yaml で上書きされる前提）
    t = raw.get("training", {})
    training = {
        "seq_len": t.get("seq_len", 1024),
        "max_lr_2d": t.get("max_lr_2d", 3e-4),
        "max_lr_1d": t.get("max_lr_1d", 3e-3),
        "batch_size_seqs": t.get("batch_size_seqs", 16),
        "warmup_ratio": t.get("warmup_ratio", 0.03),
        "weight_decay": t.get("weight_decay", 0.1),
        "beta2": t.get("beta2", 0.95),
        "grad_clip": t.get("grad_clip", 1.0),
    }
    
    # トップレベルマージ
    return {
        **raw,  # seed, data_path, tokenizer_path, max_steps, num_epochs, etc.
        "model_params": model_params,
        "hpo": training,  # 互換性のため hpo キーも残す
        **training,       # フラットにも展開（TrainingArguments直渡し用）
    }


def _detect_vram() -> float:
    try:
        if torch.cuda.is_available():
            return round(torch.cuda.get_device_properties(0).total_memory / 1024**3, 2)
    except Exception:
        pass
    return 4.0
```

#### `configs/config.yaml` 新フォーマット
```yaml
# @package _global_
# ベース設定（変更頻度低）
defaults:
  - config        # 自分自身（継承ベース）
  - hparams_150M  # モデルサイズ固有の最適HP（自動生成される）

# 共通固定値
seed: 42
precision: "bf16"
output_dir: "models/output"

# データ
data_path: "data/dataset.jsonl"
val_data_path: null  # 存在すれば評価有効化
tokenizer_path: "data/tokenizer.json"

# 学習制御（hparamsで上書きされる値のデフォルト・フォールバック）
training:
  seq_len: 1024
  max_steps: -1
  num_epochs: 3
  save_steps: 1000
  eval_steps: 1000
  logging_steps: 10
  drive_upload_interval: 1000

# モデルアーキテクチャ（スケール変更時のみ書き換え）
model:
  target_params: 150_000_000
  llama:
    hidden_size: 768
    num_hidden_layers: 12
    num_attention_heads: 12
    num_key_value_heads: 3
    intermediate_size: 3072
    rope_theta: 10000.0
    vocab_size: 64000
```

#### `configs/hparams_150M.yaml` 生成フォーマット
```yaml
# @package _global_
# Generated by scripts/find_hparams.py at 2026-07-12T15:30:00
# Model: 150M | Data: 1.2B tokens | VRAM: 4GB

training:
  max_lr_2d: 2.8e-4
  max_lr_1d: 2.5e-3
  batch_size_seqs: 16
  warmup_ratio: 0.035
  weight_decay: 0.1
  beta2: 0.95
  grad_clip: 1.0

# 計算済み派生値（main.pyで再計算しても同じになる値）
per_device_batch_size: 1
grad_accum_steps: 16
```

### 2.4 `scripts/find_hparams.py` 新規作成仕様

```python
#!/usr/bin/env python3
"""
Offline Hyperparameter Search Script
分離された探索フェーズ。学習パイプライン(main.py)からは完全独立。

Usage:
  python -m scripts.find_hparams --model-size 150M --data-path data/dataset.jsonl --output configs/hparams_150M.yaml --n-trials 20
"""

import argparse
import yaml
from pathlib import Path

import optuna
from src.step_law import compute_hpo_for_target
from src.hpo_manager import objective, create_search_space
from src.config import detect_vram, load_yaml, normalize_config
from src.logger import logger


def parse_args():
    p = argparse.ArgumentParser(description="Offline HPO for LLM Training")
    p.add_argument("--model-size", required=True, choices=["50M", "150M", "3B", "7B"],
                   help="Target model size (determines architecture)")
    p.add_argument("--data-path", required=True, help="Path to training dataset (JSONL)")
    p.add_argument("--output", required=True, help="Output YAML path (e.g., configs/hparams_150M.yaml)")
    p.add_argument("--n-trials", type=int, default=20, help="Optuna trials")
    p.add_argument("--vram-gb", type=float, help="Override VRAM detection")
    p.add_argument("--seq-len", type=int, default=1024, help="Sequence length")
    return p.parse_args()


def get_base_config(model_size: str) -> dict:
    """モデルサイズごとのベースアーキテクチャ設定"""
    configs = {
        "50M":  {"n_params": 50_000_000,  "hidden": 640, "layers": 10, "heads": 10, "kv_heads": 10, "ffn": 2560},
        "150M": {"n_params": 150_000_000, "hidden": 768, "layers": 12, "heads": 12, "kv_heads": 3,  "ffn": 3072},
        "3B":   {"n_params": 3_000_000_000, "hidden": 2560, "layers": 28, "heads": 20, "kv_heads": 20, "ffn": 10240},
        "7B":   {"n_params": 7_000_000_000, "hidden": 4096, "layers": 32, "heads": 32, "kv_heads": 32, "ffn": 11008},
    }
    return configs[model_size]


def estimate_tokens(data_path: str) -> int:
    """データセットトークン数概算"""
    import json
    count = 0
    with open(data_path) as f:
        for line in f:
            count += 1
    return count * 1024  # 1行≒1024トークン想定


def main():
    args = parse_args()
    
    # 1. ベース設定構築
    arch = get_base_config(args.model_size)
    n_tokens = estimate_tokens(args.data_path)
    vram = args.vram_gb or detect_vram()
    
    logger.info(f"HPO Search: {args.model_size} | Tokens: {n_tokens:,} | VRAM: {vram}GB | Trials: {args.n_trials}")
    
    # 2. Step Law で初期値・探索空間取得
    step_law_hpo = compute_hpo_for_target(
        n_params=arch["n_params"],
        n_tokens=n_tokens,
        seq_len=args.seq_len
    )
    
    # 3. Optuna Study
    search_space = create_search_space(step_law_hpo, vram)
    
    study = optuna.create_study(direction="minimize", sampler=optuna.samplers.TPESampler(seed=42))
    study.optimize(
        lambda trial: objective(trial, arch, args.data_path, args.seq_len, vram),
        n_trials=args.n_trials,
        timeout=3600,  # 1時間制限
    )
    
    best = study.best_params
    logger.info(f"Best params: {best}")
    logger.info(f"Best value (loss): {study.best_value:.4f}")
    
    # 4. 派生パラメータ計算（main.pyで再計算不要な値も含める）
    per_device = min(best["batch_size_seqs"], 
                     1 if vram <= 4.5 else (4 if vram <= 8.5 else 8))
    grad_accum = max(1, best["batch_size_seqs"] // per_device)
    
    output = {
        "training": {
            **best,
            "per_device_batch_size": per_device,
            "grad_accum_steps": grad_accum,
        }
    }
    
    # 5. YAML出力
    Path(args.output).parent.mkdir(parents=True, exist_ok=True)
    with open(args.output, "w") as f:
        yaml.dump(output, f, default_flow_style=False, sort_keys=False)
    
    logger.info(f"Saved to {args.output}")


if __name__ == "__main__":
    main()
```

### 2.5 `src/hpo_manager.py` ライブラリ化（インターフェース定義）

```python
"""HPO Library: Optuna objective & search space definition.
main.py からは import されない。scripts/find_hparams.py からのみ使用。
"""

import optuna
import torch
from transformers import Trainer, TrainingArguments
from datasets import load_dataset

from src.model_utils import create_model, estimate_model_size
from src.logger import logger


def create_search_space(step_law_hpo: dict, vram_gb: float) -> dict:
    """Step Law結果を中心とした探索空間定義"""
    lr_center = step_law_hpo["max_lr_2d"]
    return {
        "max_lr_2d": (lr_center * 0.5, lr_center * 2.0, "log"),
        "max_lr_1d": (lr_center * 5, lr_center * 20, "log"),
        "batch_size_seqs": [8, 16, 32],
        "warmup_ratio": (0.01, 0.1),
        "weight_decay": (0.01, 0.3),
        "beta2": (0.9, 0.99),
        "grad_clip": (0.5, 2.0),
    }


def objective(trial: optuna.Trial, arch: dict, data_path: str, seq_len: int, vram_gb: float) -> float:
    """Proxy training objective (short run, small data fraction)"""
    
    # Sample hyperparams
    hpo = {}
    space = create_search_space({}, vram_gb)  # baseline
    for param, spec in space.items():
        if isinstance(spec, list):
            hpo[param] = trial.suggest_categorical(param, spec)
        elif spec[2] == "log":
            hpo[param] = trial.suggest_float(param, spec[0], spec[1], log=True)
        else:
            hpo[param] = trial.suggest_float(param, spec[0], spec[1])
    
    # Build minimal config for proxy training
    config = {
        **arch,
        "hpo": hpo,
        "data_path": data_path,
        "seq_len": seq_len,
        "max_steps": 50,  # Proxy steps
        "data_fraction": 0.005,
        "precision": "bf16",
        "vram_limit_gb": vram_gb,
        "seed": 42,
    }
    
    try:
        # Quick proxy training (reuse train_model logic but minimal)
        from src.train_model import train as proxy_train  # 既存関数流用、またはここでインライン実装
        loss = proxy_train(config)
        return loss if not (torch.isnan(torch.tensor(loss)) or torch.isinf(torch.tensor(loss))) else 1e9
    except Exception as e:
        logger.warning(f"Trial failed: {e}")
        return 1e9
```

### 2.6 `src/drive_uploader.py` 強化要件（維持・改良）

現状維持で良いが、以下を確認・追加：
- [ ] **サービスアカウント認証対応**（`GOOGLE_APPLICATION_CREDENTIALS` 環境変数優先）
- [ ] **リトライ・指数バックオフ**（ネットワーク不安定対策）
- [ ] **アップロード失敗時も学習継続**（`try-except` で `TrainerCallback` をクラッシュさせない）
- [ ] **ローカル削除オプション**（アップロード成功後、古いチェックポイントをローカルから削除してSSD節約）

### 2.7 `src/model_utils.py` 整理・必須関数

```python
"""Model Utilities: 初期化・DeepSpeed設定生成・パラメータ推定"""

from transformers import LlamaConfig
import torch
import json


def create_model_config(config: dict, tokenizer) -> LlamaConfig:
    """config dict から LlamaConfig 生成（hidden_size調整込み）"""
    mp = config["model_params"]
    hidden = mp["hidden_size"]
    heads = mp["num_attention_heads"]
    # hidden_size を heads の倍数に調整
    adjusted_hidden = (hidden // heads) * heads
    
    return LlamaConfig(
        vocab_size=mp["vocab_size"],
        hidden_size=adjusted_hidden,
        intermediate_size=mp["intermediate_size"],
        num_hidden_layers=mp["num_hidden_layers"],
        num_attention_heads=heads,
        num_key_value_heads=mp["num_key_value_heads"],
        rope_theta=mp["rope_theta"],
        pad_token_id=tokenizer.pad_token_id,
        bos_token_id=tokenizer.bos_token_id,
        eos_token_id=tokenizer.eos_token_id,
        use_cache=False,  # training用
    )


def estimate_model_size(model) -> int:
    """モデルパラメータ数推定（埋め込み込み）"""
    return sum(p.numel() for p in model.parameters())


def generate_deepspeed_config(n_params: int, vram_limit_gb: float, precision: str = "bf16") -> str:
    """DeepSpeed JSON生成→ファイル保存→パス返却"""
    # 既存 train_model.py のロジックをほぼそのまま移植
    # 変更点: ファイル名は固定せず tempfile 使用、または output_dir 直下に配置
    import tempfile
    
    est_vram = (n_params * 14) / (1024**3)  # BF16+Optimizer+Grad概算
    
    if precision == "bf16":
        precision_cfg = {"bf16": {"enabled": True}}
    else:
        precision_cfg = {"fp16": {"enabled": True, "loss_scale": 0}}
    
    # ZeRO Stage決定
    if est_vram > vram_limit_gb * 0.9:
        zero = {"stage": 3, "offload_param": {"device": "cpu"}, "offload_optimizer": {"device": "cpu"}}
    elif vram_limit_gb <= 5.0:
        zero = {"stage": 1}
    else:
        zero = {"stage": 2}
    
    ds_config = {
        **precision_cfg,
        "zero_optimization": zero,
        "gradient_accumulation_steps": "auto",
        "train_batch_size": "auto",
        "gradient_clipping": 1.0,
    }
    
    fd, path = tempfile.mkstemp(prefix="ds_config_", suffix=".json", dir=".")
    with open(path, "w") as f:
        json.dump(ds_config, f, indent=2)
    return path
```

### 2.8 `src/env_snapshot.py` 簡素化

```python
"""Environment Snapshot: 再現性のための最小限メタデータ取得"""

import subprocess
import sys
import torch
import platform


def capture_env_snapshot() -> dict:
    """MLflow artifact用の軽量スナップショット"""
    snap = {
        "python": sys.version.split()[0],
        "platform": platform.platform(),
        "torch": torch.__version__,
        "cuda": torch.version.cuda if torch.cuda.is_available() else "none",
        "gpu": torch.cuda.get_device_name(0) if torch.cuda.is_available() else "none",
    }
    
    # Git hash
    try:
        snap["git_hash"] = subprocess.check_output(
            ["git", "rev-parse", "HEAD"], stderr=subprocess.DEVNULL
        ).decode().strip()
        snap["git_dirty"] = bool(subprocess.check_output(
            ["git", "status", "--porcelain"], stderr=subprocess.DEVNULL
        ).decode().strip())
    except Exception:
        snap["git_hash"] = "unknown"
    
    # Key packages
    for pkg in ["transformers", "datasets", "accelerate", "deepspeed", "tokenizers"]:
        try:
            import importlib.metadata
            snap[pkg] = importlib.metadata.version(pkg)
        except Exception:
            snap[pkg] = "not_installed"
    
    return snap
```

---

## 3. 設定ファイル移行マッピング表

| 旧パス/キー | 新パス/キー | 備考 |
|---|---|---|
| `configs/scaling_150m.yaml` | `configs/hparams_150M.yaml` (生成物) | 手動編集禁止・スクリプト生成 |
| `config.yaml` → `training.max_lr_2d` | `hparams_150M.yaml` → `training.max_lr_2d` | `defaults` で上書き |
| `config.yaml` → `model.llama.hidden_size` | `config.yaml` に残す | アーキテクチャ変更時のみ編集 |
| `main.py` CLI `--hpo` | **削除** | `scripts/find_hparams.py` へ移管 |
| `main.py` CLI `--resume` | `config.resume_from_checkpoint` | Hydra overrideで `resume_from_checkpoint=true` |
| MLflow `mlruns/` | **削除** | TensorBoard `models/output/runs/` のみ |

---

## 4. 実行手順書（コマンドレベル）

### Phase 0: バックアップ
```bash
cd C:\Users\saiha\My_Service\programing\LLM\Novel_LLM\LLM_Training
git add -A && git commit -m "pre-refactor backup"
pip list > requirements_backup.txt
```

### Phase 1: 削除実行
```bash
git rm src/modern_gpt.py src/registry.py src/trainer.py src/scaling_analysis.py
git rm configs/scaling_50m.yaml configs/scaling_150m.yaml configs/scaling_3b.yaml configs/scaling_7b.yaml
```

### Phase 2: `pyproject.toml` 依存関係整理
```toml
# [project.dependencies] から削除
# - "optuna>=3.0.0"
# - "mlflow>=2.10.0"

# [project.optional-dependencies] に追加
dev = [
    "pytest>=7.0",
    "ruff>=0.4.0",
    "optuna>=3.0.0",   # 移動
    "mlflow>=2.10.0",  # 移動
]
```

### Phase 3: 核心ファイル作成・書き換え
```bash
# 1. config.py 書き換え
# 2. main.py 完全書き換え（上記仕様通り）
# 3. model_utils.py 整理（generate_deepspeed_config, create_model_config 等）
# 4. env_snapshot.py 簡素化
# 5. configs/config.yaml 書き換え（defaults追加）
# 6. configs/hparams_150M.yaml 新規作成（暫定値）
# 7. scripts/find_hparams.py 新規作成
# 8. drive_uploader.py 確認・強化
```

### Phase 4: 動作確認
```bash
# 1. 設定読み込みテスト
python -c "from src.config import load_config; import hydra; hydra.initialize_config_dir('../configs'); cfg=hydra.compose('config'); print(load_config(cfg))"

# 2. ドライラン（1ステップ）
python -m src.main max_steps=1

# 3. HPOスクリプト単体テスト
python -m scripts.find_hparams --model-size 150M --data-path data/dataset.jsonl --output configs/hparams_150M.yaml --n-trials 3

# 4. 学習再開テスト
python -m src.main resume_from_checkpoint=models/output/checkpoint-1000
```

---

## 5. 検証チェックリスト（完了定義の詳細）

### 5.1 機能検証
- [ ] `python -m src.main max_steps=10` がエラーなく完走し、TensorBoardに `train/loss` が記録される
- [ ] `models/output/` に `config.json`, `pytorch_model.bin` (または `.safetensors`), `tokenizer.json` が生成される
- [ ] Google Drive 指定フォルダに `checkpoint-XXXX.zip` がアップロードされる
- [ ] 学習中断→`resume_from_checkpoint` 指定で再開時、ステップ数・Lossが継続される
- [ ] `scripts/find_hparams.py` 実行で `configs/hparams_150M.yaml` が更新され、中身が妥当な数値（LR 1e-4〜1e-3等）である

### 5.2 非機能検証
- [ ] `src/` 以下 `.py` ファイル数 ≤ 10
- [ ] `main.py` 行数 ≤ 250（コメント・空行除く）
- [ ] `pip install -e .` 後、`optuna` `mlflow` を import しないで `main.py` が動く
- [ ] 起動〜学習開始までのリードタイム ≤ 10秒（HPO待ちなし）
- [ ] VRAM 4GB環境で `seq_len=1024, batch=1, grad_accum=16` で OOM なし（ZeRO-3 CPU offload動作）

### 5.3 コード品質
- [ ] `ruff check .` エラー0
- [ ] `ruff format .` 適用済み
- [ ] 未使用import・デッドコードなし

---

## 6. リスク詳細とロールバック手順

| リスク | 兆候 | 即時対応 | ロールバック |
|---|---|---|---|
| `LlamaForCausalLM` 移行でLoss発散 | Step 100でLoss > 10 / NaN | LR下げる（`max_lr_2d=1e-4`）、`grad_clip=0.5` | `git revert` Phase 2コミット |
| DeepSpeed ZeRO-3 CPU offload で極端に遅い | samples/sec < 0.1 | ZeRO-2固定、または `offload_param.device="nvme"` | `generate_deepspeed_config` で stage=2 強制 |
| Drive API quota exceeded | アップロード失敗ログ連発 | `drive_upload_interval` を 5000 に増やす、手動バックアップ併用 | Callback無効化（`callbacks=[]`） |
| Hydra `defaults` マージ順バグ | 設定値が意図と異なる | `python -m src.main --cfg job` で解決済みconfig確認 | `config.yaml` から `defaults` 削除し手動マージ |

---

## 7. 今後の拡張ポイント（実装しないが設計で担保）

| 拡張 | 対応箇所 | 備考 |
|---|---|---|
| 複数GPU (DDP) | `main.py` `Trainer` 引数追加のみ | `TrainingArguments` 自動対応 |
| 異なるアーキテクチャ (Mamba, Mixtral) | `model_utils.create_model_config` 分岐追加 | `model.arch` キーで分岐 |
| W&B 連携 | `report_to=["wandb"]` 追加のみ | 依存追加不要（HF Trainer標準） |
| 推論最適化 (vLLM) | 別リポ推奨 | 学習コードに含めない |

---

## 8. 変更履歴

| 日付 | バージョン | 内容 | 担当 |
|---|---|---|---|
| 2026-07-12 | 1.0 | 初版作成 | - |
| 2026-07-12 | 2.0 | 詳細仕様・コードレベル仕様・コマンド手順追加 | - |

---
*本ドキュメントは実装中随時更新される。実装完了後に「実装完了報告書」として最終版をタグ付けする。*