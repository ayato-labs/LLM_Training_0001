# ADR-001: ハイパーパラメータ管理の外部化
- 日付: 2026-06-23
- ステータス: 実装中

## 背景
現在、トレーニングのハイパーパラメータやモデル設定が `src/train_model.py` 内にハードコードされている。これにより、設定変更のたびにコードを編集する必要があり、実験と設定値の紐付けが困難である。

## 決定
設定（モデル定義、ハイパーパラメータ）を `config/config.yaml` に分離し、実行時に読み込む構成とする。

## 理由
設定とロジックを分離することで、設定のバージョン管理が容易になり、実験の再現性が向上するため。
